# 第一章 数据结构与算法笔试

设计模式1-2题

Http协议、计算机网络1-2题

数据结构与算法（排序、二叉树遍历查找）3-4题



## 1.1 二叉树知识点

二叉树遍历：https://www.hello-algo.com/chapter_tree/binary_tree_traversal/

如果是**二叉树**，那么必须要先序+中序，或者中序+后序可以确定一棵树，树的话一般都可以直接确定



**例题一：已知后序、中序，求前序**

已知某二叉树的后序遍历序列是dabec，中序遍历序列是deabc，它的前序遍历序列是什么？

> 关键点一：后序遍历的最后一个节点是根节点
>
> 关键点二：由中序遍历加根节点，可以一分为二分别求左右两个树的，由此可不断简化

由题可推出 c 的左节点树后序遍历为 dabe，中序遍历为 deab，右节点树为空



## 1.2 数学运算总结

**获取所有小于 n 的质数**

```js
const getPrimesArr = (n) => {
  const res = []
  const primesArr = new Array(n).fill(true)
  for (let index = 2; index < n; index++) {
    if (primesArr[index]) res.push(index)
    for (let j = index * index; j < n; j = j + index) {
      primesArr[j] = false
    }
  }
  return res
}
```



## 1.3 行测笔试记录

**同比增长率=（本年的指标值-去年同期的值）÷去年同期的值*100%** 



**日利率万 4.5，那么年（365天）利率：  X=4.5/10000 * 365 * 100% = 16.425% **



**持有期收益：`r=[D+(P1-P0)/n]/P0`** 

> D是年[现金股利](https://baike.baidu.com/item/现金股利)额，P0是股票买入额，P1是股票卖出额，n是股票持有年数。 



**数字运算技巧**

常见的有等差数列（这一项减前一项为常数）、等比数列（这一项除前一项为常数）

1，12，4，10，7，（ 8 ），10，6：分奇数项和偶数项分别查看！

1，-6，-9，0，45，198，（ 675 ）：一次求差没超出规律，再试试二次求差找出规律
